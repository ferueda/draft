{
  "name": "repository-investigator-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A scaffold for a staged TypeScript repository investigator agent project.",
  "type": "module",
  "scripts": {
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test",
    "check-local-env": "node scripts/check-local-env.mjs",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "prepare-local-env": "node scripts/prepare-local-env.mjs",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "verify": "pnpm check"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "oxfmt": "0.61.0",
    "oxlint": "1.76.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.18.0",
  "dependencies": {
    "ai": "^7.0.47",
    "zod": "^4.4.3"
  }
}
